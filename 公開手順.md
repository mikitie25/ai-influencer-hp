# 公開手順

株式会社AIインフルエンサー コーポレートサイト

---

## 1. ファイルの構成

```
ai-influencer-hp/
├── index.html          トップ（表紙＋各ページの入口）
├── about.html          会社概要
├── service.html        事業内容
├── securedev.html      SecureDev Suite（料金プランを含む）
├── development.html    受託開発
├── contact.html        お問い合わせ（フォーム）
├── privacy.html        プライバシーポリシー
├── legal.html          特定商取引法に基づく表記
├── 404.html            ページが見つからないとき
├── style.css           全ページ共通のスタイル
├── favicon.svg         ブラウザのタブに出るアイコン
├── robots.txt          検索エンジン向けの指示
├── sitemap.xml         検索エンジン向けのページ一覧
├── server.js           サーバー本体（配信＋お問い合わせ受信）
├── package.json        依存関係
├── railway.json        Railway の設定
├── Procfile            起動コマンド
├── .gitignore          Git に上げないファイルの指定
├── set-domain.sh       ドメインを一括で設定するスクリプト
└── images/             画像 11 枚
```

---

## 2. 手元で動かす

```bash
cd ~/Desktop/ai-influencer-hp
npm install
ADMIN_USER=好きなID ADMIN_PASS=好きなパスワード npm start
```

`起動しました -> http://localhost:3000` と出たら成功。
ブラウザで `http://localhost:3000` を開く。

- お問い合わせの受信一覧 → `http://localhost:3000/admin`
- CSV で書き出し → `http://localhost:3000/admin/export.csv`

止めるときは `Ctrl + C`。

---

## 3. 公開前に埋めるところ

以下は空欄のままです。**公開前に必ず埋めてください。**

### 会社情報（about.html）

| 項目 | 状態 |
|---|---|
| 代表者 | 空欄 |
| 設立 | 空欄 |
| 資本金 | 空欄 |
| 連絡先 | 空欄 |

### 連絡先（contact.html）

| 項目 | 状態 |
|---|---|
| メールアドレス | 空欄 |
| 電話番号 | 空欄 |
| 受付時間 | 空欄 |

### プライバシーポリシー（privacy.html）

| 項目 | 状態 |
|---|---|
| 個人情報保護管理者 | 空欄 |
| 連絡先 | 空欄 |
| 制定日 | 空欄 |

### 特定商取引法に基づく表記（legal.html）

| 項目 | 状態 |
|---|---|
| 代表者 | 空欄 |
| 電話番号・受付時間 | 空欄 |
| メールアドレス | 空欄 |
| お支払い方法 | 空欄 |
| お支払い時期 | 空欄 |

**特定商取引法に基づく表記は、空欄のままでの公開は法令上の不備になります。**
とくに代表者名・電話番号・メールアドレスは必須項目です。

### 埋め方の例

```bash
cd ~/Desktop/ai-influencer-hp

# 代表者（about.html と legal.html の両方）
sed -i 's|<th>代表者</th><td><span class="blank">（　　　　　　）</span></td>|<th>代表者</th><td>山田 太郎</td>|' about.html legal.html

# 設立
sed -i 's|<th>設立</th><td><span class="blank">（　　　　　　）</span></td>|<th>設立</th><td>2024年4月</td>|' about.html

# メールアドレス（contact.html）
sed -i 's|<td id="c-email">.*</td>|<td id="c-email"><a href="mailto:info@example.co.jp">info@example.co.jp</a></td>|' contact.html

# 電話番号（contact.html）
sed -i 's|<td id="c-tel">.*</td>|<td id="c-tel">06-0000-0000</td>|' contact.html

# 受付時間（contact.html）
sed -i 's|<td id="c-hours">.*</td>|<td id="c-hours">平日 10:00〜18:00</td>|' contact.html

# 埋め残しの確認（何も出なければ完了）
grep -n '（　　　　　　）' *.html
```

---

## 4. ドメインを設定する

ドメインが決まったら、これを 1 回叩くだけです。

```bash
cd ~/Desktop/ai-influencer-hp
bash set-domain.sh https://取得したドメイン
```

`残り __SITE_URL__ : 0` と出たら成功。
SNS に貼ったときの表示、検索エンジン向けの設定が一度に埋まります。

控えは `backup-日時/` に自動で保存されるので、間違えても戻せます。

---

## 5. Railway に公開する

### 5-1. GitHub に上げる

```bash
cd ~/Desktop/ai-influencer-hp
git init
git add .
git commit -m "コーポレートサイト 初版"
git branch -M main
git remote add origin https://github.com/mikitie25/リポジトリ名.git
git push -u origin main
```

`contact.db` と `node_modules` は `.gitignore` で除外済みです。

### 5-2. Railway で設定

1. Railway で New Project → Deploy from GitHub repo → 上のリポジトリを選ぶ
2. Variables に次の 3 つを登録する

| 変数名 | 値 |
|---|---|
| `NODE_ENV` | `production` |
| `ADMIN_USER` | 受信一覧を見るための ID |
| `ADMIN_PASS` | 受信一覧を見るためのパスワード（長めに） |

`PORT` は Railway が自動で設定するので、登録は不要です。

3. Settings → Networking → Custom Domain で、取得したドメインを登録する
4. ドメイン側の DNS に、Railway が表示する CNAME を設定する

### 5-3. 公開後の確認

```
https://ドメイン/            トップが出るか
https://ドメイン/contact.html フォームから実際に送ってみる
https://ドメイン/admin       受信一覧に届いているか
https://ドメイン/nope        404 ページが出るか
https://ドメイン/robots.txt  中のドメインが正しいか
```

---

## 6. お問い合わせデータについて

お問い合わせは `contact.db`（SQLite）に保存されます。

**注意。** Railway では、再デプロイのたびにファイルが初期化されます。
問い合わせを残し続けたい場合は、次のどちらかが必要です。

- Railway の Volume を追加し、環境変数 `DATA_DIR` にそのパスを設定する
- 定期的に `/admin/export.csv` から CSV で書き出して保管する

まずは後者（CSV で書き出す）で運用を始めて、件数が増えてきたら Volume を検討するのが手軽です。

---

## 7. 入っている安全対策

| 対策 | 内容 |
|---|---|
| 通信の暗号化 | Railway の証明書。http は https に転送 |
| HSTS | 本番環境でのみ有効 |
| CSP | スクリプト・スタイルの読み込み元を制限 |
| クリックジャッキング対策 | X-Frame-Options |
| MIME スニッフィング対策 | X-Content-Type-Options |
| 入力チェック | サーバー側でも全項目を検査 |
| 改ざん対策 | 選択肢の値をサーバー側で照合 |
| 自動投稿よけ | 人には見えない入力欄で判定 |
| 連投の抑止 | 同一 IP から 30 秒に 1 件まで |
| XSS 対策 | 受信一覧の表示時にエスケープ |
| 受信一覧の保護 | Basic 認証 |

---

## 8. 書体を変えたいとき

表紙の社名は 5 種類から選べます。

```bash
cd ~/Desktop/ai-influencer-hp

sed -i 's|cover-name f-[a-z]*|cover-name f-cinzel|' index.html      # 重厚
sed -i 's|cover-name f-[a-z]*|cover-name f-cormorant|' index.html   # 斜体（現在）
sed -i 's|cover-name f-[a-z]*|cover-name f-vibes|' index.html       # 筆記体
sed -i 's|cover-name f-[a-z]*|cover-name f-orbitron|' index.html    # 未来寄り
sed -i 's|cover-name f-[a-z]*|cover-name f-mont|' index.html        # 標準

# 現在の設定を確認
grep -o 'cover-name f-[a-z]*' index.html
```

叩いたあと、ブラウザで `Ctrl + Shift + R`。
